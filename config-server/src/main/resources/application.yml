spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/brokyewing/Turkcell-4.5-PROJECT.git
          default-label: master
          search-paths: 
            - configurations/
            - configurations/{application}

server:
  port: 8787